export enum Scenes {
  MainMenu,
  Adventurers,
  Banners,
  Battle,
  Quests,
}

export enum PopupWindows {
  SimpleInfo,
}

export enum AdventurerDetailsTabs {
  Statistics,
  SkillTree,
  Items,
}
