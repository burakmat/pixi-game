export enum Scenes {
  MainMenu,
  Lobby,
  Banners,
  Battle,
  Quests,
}

export enum PopupWindows {
  SimpleInfo,
}

export enum AdventurerDetailsTabs {
  Statistics,
  SkillTree,
  Items,
}

export enum AdventurerListMode {
  Inventory,
  Market
}